[package]
name = "invoker-emulator"
version = "0.1.0"
description = "Offline Dota 2 Invoker practice tool"
authors = ["Invoker Emulator Contributors"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
